def str_in_list(list):
  count = 0
  for i in list:
    if len(i) >= 2 and i[0] == i[-1]:
      count = count + 1
  return count


str_in_list = str_in_list(input("Type in a list: ").split(", "))
print(str_in_list)
